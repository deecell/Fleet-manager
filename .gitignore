node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Terraform - CRITICAL: Never commit these
.terraform/
.terraform/**
*.tfstate
*.tfstate.*
*.backup
.terraform.lock.hcl
terraform.tfvars
terraform/*.tfstate*
terraform/*.backup
terraform/.terraform/
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Replit config (contains env vars)
.replit
.replit.nix

# Secrets and credentials
*.pem
*.key
.env
.env.*
.env.local
.env.*.local
*.secret
*credentials*

# Local S3 mock (server/aws/s3.ts) — used when S3_BUCKET_NAME isn't set
.local-uploads/
*secrets*

# AWS
.aws/
aws-credentials

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/
npm-debug.log*

# OS files
Thumbs.db
